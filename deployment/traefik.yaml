
ssl:
  enabled: true
  enforced: false

acme:
  enabled: true
  staging: false
  email: "support@n1analytics.com"
  persistence:
    enabled: true
    storageClass: slow
    accessMode: ReadWriteOnce
    size: 1Gi

dashboard:
  enabled: true
  domain: "traefik.es.data61.xyz"
  auth:
    basic:
      # Use n1: "paillier" as password:
      n1: "$2y$05$Dy1R3IAMxwIB0rULZVhSHuTGdXXpyHNv1GObyH/O.k.xRIo6p9yja"

gzip:
  enabled: false